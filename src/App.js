const options = [
  'one',
  'two',
  'three'
];

export default function Select() {
  return (
    <select>
      <button>
        <selectedoption></selectedoption>
      </button>
      <option>
    </select>
  );
}
